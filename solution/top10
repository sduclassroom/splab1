#! /bin/bash 
lines=$(cut -f11 -d" " $1 | grep -v '"-"' | wc -l);
echo $lines;
per="";
for i in `cut -f11 -d" " $1 | grep -v '"-"' | sort -t" " -k1 | uniq -c | sort --reverse | head -n 10`;
do if [[ $i =~ ^[0-9]+$ ]]; then 
	per=$i;
   else printf "$i - $per - $(echo "scale=4;$per*100/$lines" | bc -l) %% \n"; fi  
done 
